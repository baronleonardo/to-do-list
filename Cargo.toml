[package]
name = "to_do_list"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
gtk = { version = "0.14" }
gdk = { version = "0.14" }
glib = { version = "0.14" }

